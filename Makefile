#
# This is a project Makefile. It is assumed the directory this Makefile resides in is a
# project subdirectory.
#

PROJECT_NAME := tft_sniff

# Update this if you want to step to new common version

#COMPONENT_SRCDIRS:=$(abspath ../../common/v0001/)
#COMPONENT_ADD_INCLUDEDIRS:=$(abspath ../../common/v001/)
#COMPONENT_SUBMODULES:=$(abspath ../../common/v001/)
#EXTRA_COMPONENT_DIRS:=$(abspath ../../common/v001/)

include $(IDF_PATH)/make/project.mk







